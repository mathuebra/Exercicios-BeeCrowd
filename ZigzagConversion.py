class Solution:
    def convert(self, s: str, numRows: int) -> str:
        matrix = [[] for _ in range(numRows)]
        for char in s:
            
        
        
        
        
        
print(Solution().convert(s='', numRows=4))